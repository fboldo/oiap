{
	"name": "@oiap/exporter-claude-code",
	"version": "0.0.0",
	"type": "module",
	"private": true,
	"description": "Claude Code target exporter for Open Interoperable Agent Plugins.",
	"exports": {
		".": {
			"types": "./dist/index.d.ts",
			"import": "./dist/index.js"
		}
	},
	"files": [
		"dist",
		"src"
	],
	"scripts": {
		"build": "bun run --cwd ../runtime build && tsc -p tsconfig.build.json",
		"check": "tsc -p tsconfig.json --noEmit",
		"test": "bun run build && bun test src"
	},
	"dependencies": {
		"@oiap/core": "workspace:*",
		"@oiap/runtime": "workspace:*"
	},
	"peerDependencies": {
		"typescript": "^5"
	}
}
