{
	"name": "@oiap/exporter-claude-code",
	"version": "0.0.0",
	"type": "module",
	"description": "Claude Code target exporter for Open Interoperable Agent Plugins.",
	"exports": {
		".": {
			"types": "./dist/index.d.ts",
			"import": "./dist/index.js"
		}
	},
	"files": [
		"dist",
		"src"
	],
	"publishConfig": {
		"access": "public"
	},
	"scripts": {
		"build": "tsc -p tsconfig.build.json",
		"check": "bun run typecheck",
		"test": "bun test src",
		"typecheck": "tsc -p tsconfig.json --noEmit"
	},
	"dependencies": {
		"@oiap/core": "workspace:*",
		"@oiap/runtime": "workspace:*"
	},
	"peerDependencies": {
		"typescript": "^5"
	}
}
